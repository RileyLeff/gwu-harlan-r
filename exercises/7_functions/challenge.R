# 7_functions/challenge.R -------------------------------------------------
# 1. Write a function square(x) that returns x^2.
#
# 2. Write add_percent(x, pct = 10) which increases x by the
#    given percentage (e.g. 100 → 110 when pct = 10).
#
# 3. Use sapply() with an anonymous function to divide each element
#    of 1:6 by 2, saving the result in halves.
#
# Implement your answers below, save the file, then run
# source("challenge.R") to verify.
