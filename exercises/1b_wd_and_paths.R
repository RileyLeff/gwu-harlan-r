# setwd() and getwd()
    # riley's pet peeve -- never write them into your script!
    # ok to use interactively
    # better to separate into own module
    # best to use CLI args, but no one does that :(

# absolute vs relative file path

# TODO: how does windows filesystem work?