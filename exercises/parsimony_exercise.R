my_tree <- coolpackage::generate_tree(148)

coolpackage::parsimony_model
coolpackage::random_model

fit_quality_parsimony <- fit(parsimony_model(my_tree)) # likelihood 0.8
fit_quality_parsimony <- fit(parsimony_model(my_tree)) # likelihood 0.8
