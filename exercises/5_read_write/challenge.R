# 5_read_write/challenge.R ------------------------------------------------
# Challenge: Base-R import / export
#
# 1. Read data/chillin.csv into an object cdat.
# 2. Create a new column chill_score defined as (health + hunger) / 2.
# 3. Keep only rows where location == "SEH" and write them to
#      output/chillin_seh.csv   (no row names).
# 4. Read the file back into test and confirm nrow(test) == 2.
#
# Write your code below, save, and run source("challenge.R") to check.
