# 4_data_frames/challenge.R ----------------------------------------------
# Challenge: Hands-on with rows, columns, and recycling
#
# 1. Make a data frame cars with columns
#       speed = c(60, 72, 85, 90)
#       time  = c(1.0, 0.8, 0.7, 0.6)
#    Add a third column distance that is speed * time.
#
# 2. Retrieve the second row and the distance column in a single command.
#
# 3. Replace any distance value < 60 with NA using row indexing.
#
# 4. Drop the time column using negative indexing.
#
# Save your work and run source("challenge.R") to test.
