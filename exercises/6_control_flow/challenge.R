# 6_control_flow/challenge.R ---------------------------------------------
# Challenge: Decision-making and iteration
#
# 1. Write a function classify(num) that returns
#      "low"  if num < 0,
#      "mid"  if 0 <= num < 10,
#      "high" otherwise.
#
# 2. Use ifelse() to apply classify() to vec <- c(-2, 0, 5, 12)
#    in a single line (hint: anonymous function).
#
# 3. Use a for-loop to compute a running mean of 1:5
#    and store the results in run_mean.  The first element is the
#    mean of 1, the second is the mean of 1:2, etc.
#
# Save, then run source("challenge.R") to test.
