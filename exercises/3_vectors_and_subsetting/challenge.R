# 3_vectors_and_subsetting/challenge.R ------------------------------------
# Challenge: Sub-setting skills check
#
# 1. Make vec <- c(4, 8, 15, 16, 23, 42).
#    a. Use negative indexing to drop the 3rd element.
#    b. Use logical indexing to keep only values > 20.
#
# 2. Create a named numeric vector scores with names
#    "A", "B", "C" and values 90, 85, 92.  Retrieve the score for "B".
#
# 3. Let x <- 1:6.  Add c(10, 20) to x and record the warning message.
#    Why does it appear?
#
# Write your code below, save the file, then run source("challenge.R").
