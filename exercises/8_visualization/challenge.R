# 8_visualization/challenge.R --------------------------------------------
# 1. Load mtcars (built-in).  Make a scatter plot of mpg vs wt.
# 2. Produce a histogram of the hp column with 15 bins.
# 3. Save both plots side-by-side to output/mtcars_plots.pdf
#    (hint: use pdf() and par(mfrow)).
#
# Write code below, save the file, then run source("challenge.R").
